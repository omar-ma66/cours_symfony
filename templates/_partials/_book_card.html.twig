
        <div style="border: 1px solid #ddd; border-radius: 8px;padding :15px ; margin-bottom:10px">
                <h1 style="margin:0 0 5px">{{ book.title}} </h1>
                <p style="margin: 0 ;color :#666">{{ book.author}} </p>
                <a href="{{ path('app_book_show',{id: index}) }}" style:"color:#2c3e50">Voir détails</a>
        </div>